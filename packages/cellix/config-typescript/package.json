{
	"name": "@cellix/config-typescript",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Reimplemented TypeScript config patterns (inspired by cellixjs, modernized for Node 24 + no graphql). Extension point.",
	"main": "tsconfig.base.json",
	"files": ["tsconfig.base.json"],
	"scripts": {
		"build": "mkdir -p dist && touch dist/.gitkeep",
		"lint": "node --eval \"console.log('lint for config pkg - excluded from main biome gate in verify')\"",
		"format": "node --eval \"console.log('format for config pkg - excluded')\"",
		"format:check": "node --eval \"console.log('format:check for config pkg - excluded')\""
	}
}
