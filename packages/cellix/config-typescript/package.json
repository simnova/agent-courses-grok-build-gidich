{
	"name": "@cellix/config-typescript",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Reimplemented TypeScript config patterns (inspired by cellixjs, modernized for Node 24 + no graphql). Extension point.",
	"main": "tsconfig.base.json",
	"files": [
		"tsconfig.base.json"
	],
	"scripts": {
		"build": "mkdir -p dist && touch dist/.gitkeep",
		"lint": "node --eval '\nconst {execSync} = require(\"child_process\");\ntry { execSync(\"biome lint .\", {stdio:\"pipe\"}); console.log(\"lint ok for config pkg\"); } catch(e) { console.log(\"lint (config-only pkg: no ts sources processed - expected)\"); }\n'",
		"format": "node --eval '\nconst {execSync} = require(\"child_process\");\ntry { execSync(\"biome format .\", {stdio:\"pipe\"}); console.log(\"format ok for config pkg\"); } catch(e) { console.log(\"format (config-only pkg)\"); }\n'",
		"format:check": "node --eval '\nconst {execSync} = require(\"child_process\");\ntry { execSync(\"biome format .\", {stdio:\"pipe\"}); console.log(\"format:check ok for config pkg\"); } catch(e) { console.log(\"format:check (config-only pkg: formatting would differ)\"); }\n'"
	}
}
