{
	"name": "@cellix/mongoose-seedwork",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Minimal mongoose seedwork reimplemented for axc (inspired by cellix patterns; not full port). Provides base entity/aggregate stubs.",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc --build",
		"clean": "rimraf dist",
		"lint": "biome lint",
		"format": "biome format --write",
		"format:check": "biome format ."
	},
	"peerDependencies": {
		"mongoose": "catalog:"
	},
	"devDependencies": {
		"typescript": "catalog:",
		"@types/node": "catalog:"
	}
}
