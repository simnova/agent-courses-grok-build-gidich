/**
 * @axc/axc/rest
 * Hono-based REST layer for the axc core.
 * Re-exports healthcheck with extension points for future metrics collection.
 */
export * from './healthcheck.js';

// Example placeholder for other routes (not implemented in initial scaffold)
export const placeholderRestNote = 'Add experiment/run routes here with same arch boundaries';
