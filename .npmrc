# Strict supply-chain policy enforcement
# Zero built dependencies approved by default.
onlyBuiltDependencies=[]
engine-strict=true
ignore-scripts=true
# Note: installs in this project use pnpm install --ignore-scripts explicitly where needed to honor policy.
# allowBuilds / onlyBuiltDependencies maintained in pnpm-workspace.yaml
